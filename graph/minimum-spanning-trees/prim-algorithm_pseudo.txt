MST-PRIM(G, w, r)
  for each u ∈ G.V
    u.key = ∞
    u.π = NIL
  r.key = 0
  Q = G.V
  while Q ≠ Φ
    u = EXTRACT-MIN(Q)
    for each v ∈ G.Adj[u]
      if v ∈ Q and w(u, v) < v.key
        v.π = u
        v.key = w(u, v)